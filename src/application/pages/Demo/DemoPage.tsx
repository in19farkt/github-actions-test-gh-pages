import * as React from 'react';
import { Typography } from 'components';

interface IProps {}

function DemoPage(_props: IProps) {
  return (
    <div>
      <Typography>Page for developers</Typography>
    </div>
  );
}

export default DemoPage;
